package com.access.control.status;

//enum usedt give feedback to the users whether their current action was successful or failed.
public enum Status {
    SUCCESS,
    USER_ALREADY_EXISTS,
    FAILURE
}
